#Mở folder source code PBL5 trong vscode

#Tạo môi trường conda sử dụng anaconda prompt
conda create -n py311 python=3.10
conda activate py311
- Lưu ý nếu muốn cài gpu thì chạy thêm 2 câu lệnh dưới
conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
python -m pip install "tensorflow=2.10"

- Kiểm tra conda đã có GPU 
import tensorflow as tf
if (len(tf.config.list_physical_devices('GPU')) > 0):
    print(f"Đã có GPU: {tf.config.list_physical_devices('GPU')}")


#Chạy file setup_envir.py
- Chuyển interpreter sang môi trường conda tên py311
- pip install wget
- mở comment Phần Install Tensorflow Object Detection, Protobuf and other dependencies và Chạy file setup_envir.py
- sau đó đóng comment phần Install Tensorflow Object Detection, Protobuf and other dependencies, mở comment phần Verify Installation và tiếp tục chạy file setup_envir.py
- Khi xác thực nếu gặp lỗi thì xem file error.txt để fix các lỗi, fix được lỗi nào thì tiếp tục chạy phần Verify Installation cho đến khi màn hình terminal báo
----------------------------------------------------------------------
Ran 24 tests in 25.119s

OK (skipped=1)

là đã cấu hình các thư viện cần thiết thành công

- Ta chạy file test_modelOCR.py để kiểm tra còn thiếu thư viện nào và fix nó